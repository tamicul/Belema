services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: belema
      POSTGRES_PASSWORD: belema
      POSTGRES_DB: belema
    ports:
      - "5432:5432"
    volumes:
      - belema_pgdata:/var/lib/postgresql/data

volumes:
  belema_pgdata:
